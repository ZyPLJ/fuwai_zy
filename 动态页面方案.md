# 「动态」页面方案（基于 Twikoo 评论系统）

> 更新：2026-09-03 ｜ 适用版本：twikoo 1.7.20 ｜ 页面：`/moments/`

## 一、背景与目标

博客（Fuwari/Astro 静态站）已有 Twikoo 评论。目标：新增导航顶层页面「动态」（`/moments/`）：

- 站长**在页面内直接发布动态**，无需后端开发、无需重新部署站点
- 访客可对**每条动态**评论/回复（嵌套回复、邮件通知、表情图片），回复精准关联到对应动态
- 仅站长可发布（Twikoo 管理员登录态检测），访客看不到发布入口

## 二、总体设计

**核心思路**：动态 = Twikoo 中站长（`master===true`）在专用 path 发表的评论；每条动态的评论区 = 该动态专属 path 上的 Twikoo 组件。

| 机制 | 实现 |
|---|---|
| 动态存储 | 站长在专用 path `/moments/publish/` 发表的评论 |
| 动态列表 | 前端 POST Twikoo 后端 `COMMENT_GET` 事件，过滤 `master===true`；`before`（上一批最旧一条的 created 时间戳）游标分页 |
| 发布 | 页面内嵌原生 Twikoo 组件（path=`/moments/publish/`），CSS 隐藏其自带评论列表只留输入区；`onCommentLoaded` 触发列表刷新（防抖 800ms） |
| 每条动态的评论区 | 该动态专属 path `/moments/#m-<评论id>` 上的 Twikoo 组件；评论数用 `GET_COMMENTS_COUNT` 批量获取（includeReply: true） |
| 发布权限 | `GET_CONFIG`（带 `localStorage["twikoo-access-token"]`）返回 `IS_ADMIN===true` 时才显示发布入口 |
| 深链 | `评论：N` 按钮旁的复制深链已移除，但深链能力保留：twikoo 回复的邮件通知链接为 `/moments/#m-<id>`，页面读 hash 自动滚动 + 高亮（3 秒主题色描边）+ 展开对应评论区 |

### Twikoo HTTP API（已实测，POST 到 envId，Content-Type: application/json）

| 事件 | 请求 | 响应要点 |
|---|---|---|
| `COMMENT_GET` | `{event, url, before?}` | `{data:[{id, nick, mailMd5, comment(HTML), master, created(ms), replies[], ...}], more, count}` |
| `GET_COMMENTS_COUNT` | `{event, urls[], includeReply}` | `{data:[{url, count}]}` |
| `GET_CONFIG` | `{event, accessToken?}` | `{config:{..., IS_ADMIN}}` |

注意：twikoo.min.js 公开 SDK 只有 `init / getCommentsCount / getRecentComments`；`COMMENT_GET` 需自行 fetch。

## 三、文件清单

**新建**

- `src/pages/moments.astro` — 薄壳页面：`MainGridLayout` + `<MomentsPanel client:only="svelte" avatar={avatar.src} />`；`momentsConfig.enable` 或评论关闭时显示「动态功能未开启」占位卡。**不加**底部 `<Comment />`
- `src/components/MomentsPanel.svelte` — 全部交互逻辑（核心组件）

**修改**

- `src/types/config.ts` — `LinkPreset` 枚举加 `Moments = 6`
- `src/constants/link-presets.ts` — `[LinkPreset.Moments]: { name: i18n(I18nKey.moments), url: "/moments/" }`
- `src/config.ts` — `navBarConfig.links` 在 `About` 后插入 `LinkPreset.Moments`；新增 `momentsConfig = { enable: true, pageSize: 10, publishPath: "/moments/publish/" }`
- `src/i18n/i18nKey.ts` — 加 `moments = "moments"`；`src/i18n/languages/` 10 个语言文件各加一行翻译
- `src/layouts/Layout.astro` — 末尾随 `twikoo-loader.js` 一起加载 `public/lib/twikoo-style-keeper.js`（样式常驻器，见踩坑 #1；不再在 head 静态引入 twikoo.css，2026-09-04 起改为常驻器方案）

## 四、关键实现（MomentsPanel.svelte）

### 4.1 单实例面板互斥（最重要的架构约束）

**twikoo 1.7 的两个硬限制（源码实证）**：

1. init 选项挂载在 `Vue.prototype.$twikoo`（全局原型），所有组件实例刷新/加载更多/**发表**时实时读取 `this.$twikoo.path` —— **最后初始化的组件的 path 对全局生效**。多实例并存时：在 A 里刷新会拉到 B 的评论；展开 A 的评论后发布动态，内容会被发进 A 的评论线程。
2. 挂载用 Vue 2 的 `$mount(el)`，会**整体替换**传入的容器节点 —— 替换后的 DOM 不再归 Svelte 管，靠删除节点无法销毁组件。

**因此采用 `activePanel` 单实例面板模式**（`null | "publish" | 动态id`）：

- 同一时间只存在一个 twikoo 实例：发布框 或 某条动态的评论区（手风琴式互斥）
- 面板切换/展开时：清空旧面板内部 DOM，以正确的 path **全新 init**（保证全局 path 指向当前操作的面板）
- 站长默认自动展开发布框；展开某条评论后发布框临时收起，点「展开编辑」切回

### 4.2 面板挂载 action（`panelMount`）

- **Svelte 只持有外层 wrapper**（twikoo 替换的是 wrapper 的内层节点，wrapper 永远归 Svelte 管），显隐用 wrapper 的 `display:none`（scoped 类 `thread-hidden`）切换 → 收起一定藏得住，不残留
- 每次激活：`node.innerHTML = ""` 清空 + 创建自增唯一 id 的内层节点（`twikoo-panel-<seq>`）+ 全新 `twikoo.init`
- 异步回调（`loadTwikoo()` 之后）检查 `inner.isConnected`，面板已切换的过期实例自动作废 → 快速点击/竞态不可能产生重复或嵌套
- `onCommentLoaded`：发布面板 → `refreshMoments()`（防抖刷新列表）；评论面板 → `fetchCounts()` + PhotoSwipe 灯箱重建
- `mounted` 标志防止同一激活周期内重复 init

### 4.3 列表与分页

- `fetchMomentsBatch(before, append)`：`before` 游标作用于**含垃圾评论的原始评论流**，批内循环过滤 `master===true` 凑满 `pageSize` 条为止，`RAW_PAGE_LOOP_LIMIT=10` 防垃圾评论稀释时死循环，`dedupeById` 兜底
- 深链定位 `handleHash()`：按 hash 找到动态 → 展开（`activePanel = id`）、`scrollIntoView`、高亮；找不到且还有更多 → 递归 loadMore 继续
- 其余：`fetchCounts()`（SDK `getCommentsCount`，正则 `/#m-(.+)$/` 回填，失败静默）、`refreshMoments()`（防抖重拉，不闪骨架屏）、相对时间格式化（>7 天用 `formatDateI18n`）

## 五、踩坑记录（重要，改相关代码前必读）

1. **swup 切页会清掉 JS 动态注入的 `<head>` 样式**（`updateHead: true`）：twikoo.min.js 的样式是 JS 注入的（vue-style-loader 在脚本执行时把整套组件 css 以 `<style>` 写进 head），脚本只执行一次，切页后注入样式被 swup 的 HeadPlugin diff 清掉且不会复活（表现为评论区裸奔）。**为什么友链/文章页评论区不裸奔**：`Comment.astro` 的内联脚本在 swup 容器内，每次切页都会重跑 → 重新 append `twikoo.min.js` 并重新执行 → 样式重新注入；而 `MomentsPanel` 只在全局无 twikoo 时加载一次脚本（`loadTwikoo` 的 `w.twikoo` 守卫），之后每次访问只 `twikoo.init`，注入样式被清后永不回来。**方案沿革（2026-09-03~09-04）**：① 静态引入外部 `twikoo.css`（曾用 `registry.npmmirror.com`）→ 该域名有 Referer 防盗链（Tengine `denied by Referer ACL`），浏览器请求带 Referer 收到 403 HTML → 被 Chromium ORB 拦截（`net::ERR_BLOCKED_BY_ORB`），css 从未生效（实测 `.tk-comments-title` 切页后 20px/700 → 16px/400），纯靠运行时注入撑场。② 换同源 `/lib/twikoo.css` 或自建 CDN 可绕过防盗链，但仍有「css 版本必须与前端 min.js 配套」的脆弱点（前端 1.6.44 时静态 css 已是 1.7.20），且多一次 40KB 外链请求。③ **最终采用样式常驻器（当前方案）**：`public/lib/twikoo-style-keeper.js` 把 twikoo 注入 head 的 `<style>` 迁移到 `<body>` 常驻 —— HeadPlugin 只 diff `<head>` 管不到 body，一次迁移永久豁免；顺带按文本去重（twikoo 每次执行都新注入一整组，丢弃重复副本，body 永远只有一份）。优点：全站统一（Comment 页与动态面板都覆盖）、样式永远与执行它的 min.js 版本一致（无第二份版本要配套）、零额外请求。代价：`<style>` 放 body 非规范位置（主流浏览器均应用其规则，实测通过），文件注释里已交代原因，勿改回 head。**注意**：Layout head 已移除静态 twikoo.css 链接，`public/lib/twikoo.css` 已删除。音乐播放器 APlayer 是同类问题先例（提交 `9dea6e7`）。
2. **twikoo 多实例 = 路径串扰 + 发布错位**（见 4.1），不要尝试同时挂多个 twikoo 组件。
3. **twikoo `$mount` 替换容器节点**：Svelte `{#if}` 删节点删不掉组件本体 → 必须 wrapper 常驻 + 手动清空重挂（见 4.2）。
4. **Svelte 会剪枝「未使用」的 scoped CSS**：`class:list={["action-btn", ...]}` 里的类名字符串不被 CSS 使用分析识别，对应样式直接不进产物（表现为按钮没有横排布局）。**修复**：动态类用 `class="action-btn"` + `class:action-btn-active={...}` 指令写法。
5. **主题里不存在的 CSS 变量**：`--border-color`、`--primary-hover`、`--text-main` 都不存在（无效变量会回退成 `currentColor`，边框变成刺眼的纯黑/白）。可用的是 `--line-color`、`--line-divider`、`--btn-content`、`--btn-regular-bg`、`--btn-plain-bg-*`、`--text-primary`、`--text-secondary` 和 `.text-90/.text-75/.text-50` 工具类。
6. **Tailwind v3 任意值 + var() 不支持透明度修饰**：`ring-[var(--primary)]/40` 不会生成任何类；用 `ring-[var(--primary)]` 即可。
7. 组件内图标用 `@iconify/svelte`（`<Icon>`），样式尺寸交给 scoped CSS 或确认生成的 Tailwind 类。
8. 动态卡片作者头像由 `moments.astro` 编译期 `import avatar from "@/assets/images/logo_2.jpg"` 后以 `avatar.src` 字符串 prop 传入（client:only 组件拿不到 Astro 图片服务）。

## 六、升级与回退

**twikoo 版本升级**（当前 1.7.20）需同步修改 4 处 URL（均指向 `cdn.pljzy.top/twikoo.min.js`，换的是 CDN 文件本体）：

1. `src/components/Comment.astro`（twikoo.min.js）
2. `public/lib/twikoo-loader.js`（twikoo.min.js）
3. `src/components/MomentsPanel.svelte`（`TWIKOO_CDN`）
4. `public/lib/twikoo-style-keeper.js` **无需改**（无版本号，样式跟随 min.js 注入内容自动配套）

⚠️ 前端版本提示：若评论区显示「前端版本 x.x.x < 云函数版本」说明 `cdn.pljzy.top/twikoo.min.js` 文件本体没跟着换（2026-09-04 曾滞留 1.6.44）。

**回退**：`src/config.ts` 里 `momentsConfig.enable = false` → 页面显示「动态功能未开启」占位；再删 `navBarConfig.links` 里的 `LinkPreset.Moments` 可彻底下线导航入口。

## 七、站长使用流程

1. 一次性准备：在任意文章评论区输入暗号（`HIDE_ADMIN_CRYPT`，当前为 `ZY`）唤出管理面板并登录管理员
2. 访问 `/moments/` → 发布框自动展开 → 输入内容（支持 markdown/图片上传（兰空图床）/表情）→ 发表 → 约 1 秒后新动态置顶
3. 删除动态：任意评论区管理面板 → 评论管理 → 找到 path 为 `/moments/publish/` 的评论删除
4. 访客交互：点某条动态的「评论：N」展开评论区 → 嵌套回复/表情/图片，回复会触发邮件通知，邮件里的链接可直达该条动态

## 八、验证清单

- [ ] `pnpm check`（0 错误）、`pnpm lint`（biome：tab + 双引号）、`pnpm build` 通过
- [ ] 导航栏「动态」入口（桌面 + 移动端汉堡菜单），顺序：主页/归档/关于/动态/社交/其他
- [ ] 访客：无发布框；列表/计数/相对时间正常；反复点「评论」展开/收起/展开始终只有一份评论区
- [ ] 评论区串扰：在 B 评论 → 展开 A → A 内刷新 → 只显示 A 的评论
- [ ] 站长：展开某评论区后点「展开编辑」发布 → 新动态置顶且不出现在任何评论线程
- [ ] 深链：复制 `/moments/#m-<id>` 新开 → 滚动 + 高亮 + 自动展开
- [ ] swup 压测：各页来回切换多次，评论区样式正常、`twikoo.min.js` script 标签唯一
- [ ] 硬加载 `/links/` → swup 切到 `/moments/` → 展开任一条动态评论区：`.tk-comments-title` 计算样式为 20px/700（对照裸奔态 16px/400）；DevTools 检查 head 无 twikoo 注入 `<style>`、`<body>` 末尾有且仅有一组（可顺带观察切回文章页后不叠加第二组）
- [ ] 常驻器单体：`/lib/twikoo-style-keeper.js` 加载无报错；手动往 head 注入含 `.tk-` 的 `<style>` 两次 → body 只保留一份、head 无残留
- [ ] 明暗主题切换，卡片/徽标/按钮配色正常
